(function() {
    console.log('Hello git');
    var items = ['one', 'two', 'three', 'four'];

    for (var length = items.length, i = length-1; i >= 0; i--) {
        console.log(items[i]);
    }
})()
